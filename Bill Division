void bonAppetit(vector<int> bill, int k, int b) {
    int sum=0 ,x=0;
    bill.erase(bill.begin()+k);
    for(int i=0; i<bill.size(); i++){
        sum+=bill[i];
    }
    x=sum/2;
    if(x==b) cout<<"Bon Appetit";
    else cout<<b-x;
    

};
